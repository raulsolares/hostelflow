# HostelFlow - Variables de entorno (producción)
# Copia este archivo como .env y ajusta los valores

# Clave secreta para JWT (OBLIGATORIA en producción)
# Genera una con: python -c "import secrets; print(secrets.token_urlsafe(64))"
# Si ENV=production y HOSTELFLOW_SECRET falta o es el valor por defecto, la app no arranca
HOSTELFLOW_SECRET=hostelflow-dev-secret-2026

# Base de datos (SQLite por defecto, o PostgreSQL)
DATABASE_URL=sqlite:///./hostelflow.db

# Puerto
PORT=8000

# Orígenes permitidos para CORS (separados por coma)
ALLOWED_ORIGINS=http://localhost:8000

# Web Push (VAPID) — notificaciones push autoalojadas, sin proveedor externo.
# Si se dejan vacías, la app autogenera un par de claves EC P-256 al arrancar
# y las persiste en vapid_keys.json (raíz del repo, ignorado por git). Eso es
# suficiente para dev, pero en producción cada reinicio sin este archivo
# invalidaría las suscripciones existentes (el cliente pide re-suscribirse
# contra la nueva clave pública) — define estas 3 vars explícitamente.
#
# Genera un par de claves con:
#   python -c "from py_vapid import Vapid02 as Vapid; from py_vapid.utils import b64urlencode; from cryptography.hazmat.primitives import serialization; v = Vapid(); v.generate_keys(); print('VAPID_PRIVATE_KEY='); print(v.private_pem().decode()); print('VAPID_PUBLIC_KEY=' + b64urlencode(v.public_key.public_bytes(serialization.Encoding.X962, serialization.PublicFormat.UncompressedPoint)))"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_SUB=mailto:admin@hostelflow.local

# Facturación (Stripe) — plan único con checkout + portal de cliente.
# Si falta cualquiera de las 3, la facturación queda deshabilitada (503 en
# los endpoints /api/admin/billing/*) y el resto de la app funciona igual.
#
# 1. Crea una cuenta en https://dashboard.stripe.com (modo Test para dev).
# 2. STRIPE_SECRET_KEY: Developers → API keys → Secret key (sk_test_... en dev).
# 3. STRIPE_PRICE_ID: Product catalog → crea un producto con precio recurrente
#    → copia el id del price (price_...).
# 4. STRIPE_WEBHOOK_SECRET: Developers → Webhooks → añade un endpoint
#    apuntando a {tu_dominio}/api/webhooks/stripe con los eventos
#    checkout.session.completed, customer.subscription.updated y
#    customer.subscription.deleted → copia el "Signing secret" (whsec_...).
#    En dev local usa `stripe listen --forward-to localhost:8000/api/webhooks/stripe`.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Dominios personalizados por hotel (P7).
# APP_PRIMARY_DOMAIN: dominio al que los clientes deben apuntar su CNAME
# (ej. hostelflow.app). Se devuelve en GET /api/admin/hotel/domain-status
# como "expected_target". Vacío en dev — no se sugiere ningún target.
APP_PRIMARY_DOMAIN=

# Overview SaaS (super_admin, P7): precio mensual (USD, sin símbolo) usado
# para estimar el MRR cuando BILLING_ENABLED es False o Stripe.Price.retrieve
# falla. 0 = no se estima ingreso (mrr.amount queda en 0, source="estimate").
PLAN_PRICE_FALLBACK=0
